# Cobalt photos

Drop photos from tonight (or any trip) at Cobalt, The Restaurant into this
folder (upload via GitHub's web/mobile UI, or `git add`/commit/push) — they
show up automatically on `cobalt.html`'s collage. No filename convention
needed, same as the main `/photos/` pickup system.
