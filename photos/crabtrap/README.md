# Crab Trap photos

Drop photos from Crab Trap into this folder (upload via GitHub's web/mobile UI,
or `git add`/commit/push) — they show up automatically on `gallery.html`'s
Crab Trap section. No filename convention needed, same as the main `/photos/`
pickup system.
